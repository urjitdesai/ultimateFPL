{
  "name": "@ultimate-fpl/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --mode localhost",
    "build": "npm run build:production",
    "build:local": "tsc -b && vite build --mode localhost",
    "build:development": "tsc -b && vite build --mode development",
    "build:production": "tsc -b && vite build --mode production",
    "deploy:frontend:development": "npm --prefix .. run deploy:frontend:development",
    "deploy:frontend:production": "npm --prefix .. run deploy:frontend:production",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@hookform/resolvers": "^5.2.2",
    "firebase": "^12.4.0",
    "lucide-react": "^0.468.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-hook-form": "^7.65.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/react": "^19.1.16",
    "@types/react-dom": "^19.1.9",
    "@vitejs/plugin-react": "^5.0.4",
    "dotenv": "^17.4.2",
    "typescript": "^5.9.3",
    "vite": "^7.1.9",
    "vitest": "^3.2.4"
  }
}
